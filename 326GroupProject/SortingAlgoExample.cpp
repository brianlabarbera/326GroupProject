#include "SortingAlgoExample.h"

const string SortingAlgoExample::NAME = "AlgorithmName";

void SortingAlgoExample::SortAlgo(vector<int>& a, long long int& n) {
    // a is passed by reference so you can use . rather than ->
    // n has to be a long long int to not overflow
    if (n < 0) {
        // Execute sort
    }
    else {
        // Execute sort, increment n for each step taken
    }
}